spring:
  application:
    name: finance-analytics
  jpa:
    open-in-view: false

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health

app:
  security:
    token-encryption-key: ${WB_TOKEN_ENCRYPTION_KEY:}
